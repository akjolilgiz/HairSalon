@{
  Layout = "_Layout";
}

@using HairSalon.Models;
<div class = "container jumbotron">
<p><h1>@Model["Stylist"].name</h1></p>
</div>
@Model["Stylist"].name's Clients :
 @foreach(var client in @Model["Client"])
 {
 <ul>
  <li>@client.clientName</li>
  </ul>
 }
</br>
</br>
</br>

Specialty :
<form action='/stylists/@Model["Stylist"].id/addSpecialty' method='post'>
   <select name='specialties'>

    @foreach (var specialty in @Model["Specialties"])
    {
      <option  value=@specialty.id>@specialty.description</option>
    }
  </select>
  <button type='submit'>Add Specialty</button>
</form>

  @foreach(var specialty in @Model["Specialty"])
  {
  <ul>
    <li>@specialty.description</li>
  </ul>
  }
  </br>
  </br>
  </br>

  <form action='/stylists/@Model["Stylist"].id/delete' method="post">
    <button class = "btn btn-danger" type="submit">Delete this Stylist</button>
  </form>
  <hr>
<p><a href = "/clients/add" class = "btn btn-success">Add a client</a> |<a href="/clients" class = "btn btn-info">Back to Clients</a>| <a href = "/specialties" class = "btn btn-danger">Back to Specialties</a> | <a href="/stylists" class = "btn btn-primary">Back to Stylists</a> | <a href="/" class = "btn btn-info">Back to Home Page</a></p>
